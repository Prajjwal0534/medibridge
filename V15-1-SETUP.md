# MediBridge v15.1

Frontend-only fix.

The `Doctor + AI explanation` button is now shown directly inside each completed consultation card that has a consultation summary.

No SQL changes are required.
No Edge Function changes are required.

Deploy the unzipped v15.1 folder to Netlify.

Test:
Patient → Medical Record → completed consultation → Doctor + AI explanation.
